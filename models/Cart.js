class Cart {
  constructor(productId, productTitle, productPrice, quantity, amount) {
    this.id = productId;
    this.productTitle = productTitle;
    this.productPrice = productPrice;
    this.quantity = quantity;
    this.amount = amount;
  }
}

export default Cart;
